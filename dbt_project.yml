name: 'talk'
version: '1.0'


# this is an addition

profile: 'default'

source-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]

quoting:
  identifier: false

target-path: "target"
clean-targets:
    - "target"
    - "dbt_modules"

models:
  talk:
    materialized: table
